node_modules
dist
dist-ui
out
*.log
.DS_Store

# 运行时文件统一收纳在软件根的 devenv-data/ 目录（含配置、下载/安装数据、Electron 状态），
# 均不提交，也不在系统盘产生任何文件
devenv-data/

# Agent 工作记忆 / 技能（工具私有元数据，不入库）
.workbuddy/

# 编辑器 / 本地环境
.vscode/
.env
.env.*
.claude/
.cursor/

# 测试覆盖率
coverage/

# electron-builder 安装包产物（上传到 GitHub Releases，不入库）
release/
